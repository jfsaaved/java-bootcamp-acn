package org.accenture.day01.lessons;

// Lesson02Methods class
public class Lesson02Methods {

    /*
     * A function/method that takes an integer as input and returns the value increased by 5
     * 'int' - return data type
     * 'addFiveToNumber' - method name
     * 'int a' - parameter
     * 'return a + 5' - method body
     */
    public int addFiveToNumber(int a) {
        return a + 5;
    }

    //TODO: Write a public method that returns the sum (as integer) of two integers

    // Constructor - special method that runs when you create an object — sets up its initial state.
    public Lesson02Methods() {
        // Constructor body (can be empty or set initial values)
    }
}
