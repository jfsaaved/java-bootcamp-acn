package org.accenture.day03.lessons;

public class Lesson02Loops {

    // TODO: Loop through an int[] and print all elements using a for loop

    // TODO: Loop through a String[] and print all elements using an enhanced for loop

    // TODO: Create an int[] of 10 elements and fill it with the value 42 using a loop

    // TODO: Calculate the sum of all values in an int[] using a loop

    // TODO: Find the largest number in an int[] using a loop

    // TODO: Count how many even numbers are in an int[]

    // TODO: Create a method `boolean contains(int[] arr, int target)`
    //           that returns true if the target number exists in the array

    // TODO: Create a method that returns the average of all numbers in a double[]

    // TODO: Write code that reverses a String[] array in-place

    // TODO: Shift all elements in an int[] one position to the right (last wraps to front)
    public Lesson02Loops() {
        // For loop
        for (int i = 0; i < 5; i++) {
            // Body
        }

        // Iterating through arrays
        int[] scores = {0, 2, 3, 4, 5};
        for (int i = 0; i < scores.length; i++) {
            // Body
        }

        // While loop
        int i = 0;
        while (i < 5) {
            // Body
        }

        // Do-while loop
        int idx = 0;
        do {
            // Body
        } while (idx < 5);

        // For each loop
        int[] numbers = {1, 2, 3};
        for (int num : numbers) {
            // Body
        }

        // Iterating through arrays
        for (int score : scores) {
            // Body
        }
    }

}
